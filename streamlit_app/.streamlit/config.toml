[theme]
base = "dark"

# 🎨 Paleta personalizada BBVA vs Santander
primaryColor = "#1E90FF"              # Azul vivo (botones, acentos)
backgroundColor = "#0A192F"           # Fondo principal (azul oscuro, no negro)
secondaryBackgroundColor = "#0D1B2A"  # Paneles / tarjetas
textColor = "#E6F1FF"                 # Blanco azulado, suave para buen contraste

# ✨ Comentario extra:
# - Usa primaryColor (azul) para todo lo interactivo.
# - Añade rojo (#FF4C4C) en CSS o gráficos para resaltar predicciones o alertas.
